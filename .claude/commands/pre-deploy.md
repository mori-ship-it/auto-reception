デプロイ前の確認を行います。以下を順番に実行してください:

1. onclick 整合性チェック: html_body.txt の onclick で呼ばれる関数が src/app.js（および src/ 配下の .js）に定義されているか検証する（integrity-check と同じ手順）
2. 不整合があれば報告して停止する
3. 不整合がなければ `npm run build` を実行する
4. ビルドが成功したら「デプロイ準備完了」と報告する。失敗したらエラー内容を報告する
